{
  "_note": "Copy this file to config/credentials.json (gitignored) and fill in the keys you use. The pipeline runs without credentials — TMDB entertainment data is skipped if absent, and deep-dive generation needs the Gemini key only when triggered.",
  "tmdb": {
    "_note": "TMDB v3 API key. Get one at https://www.themoviedb.org/settings/api.",
    "api_key": "your_v3_key_here"
  },
  "gemini": {
    "_note": "Google Gemini API key for on-demand deep-dive synthesis. Get one at https://aistudio.google.com (Get API key). Free tier covers gemini-2.5-flash. Or set GEMINI_API_KEY in the environment instead.",
    "api_key": "your_gemini_key_here"
  },
  "omdb": {
    "_note": "Optional. OMDb API key enriches entertainment (ENT) deep dives with cross-platform critic scores (IMDb / Rotten Tomatoes / Metacritic). Free key (1000 req/day) at https://www.omdbapi.com/apikey.aspx. Without it, ENT dives fall back to TMDB's own score only.",
    "api_key": "your_omdb_key_here"
  }
}
